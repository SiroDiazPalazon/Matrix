# Library developed to realize operations over a matrix
# now under development...

import threading, math, time, sys 

class Matrix:
        
        def __init__(self, mtxRows):
                self.mtxRows = mtxRows
                self.lenMatrix = len(self.mtxRows)
                self.numElemRows = []
                if len(self.mtxRows) >= 0 and type(self.mtxRows) == 'list':
                        # initialize the matrix
                else:
                        # return a error message
                        # studying the case
        
        def _isSquare(self):
                index = 0
                control = False
                for i in self.mtxRows:
                        for j in a[index]:
                                # insert into the subarrays length
                                self.numElemRows = [len(x) for x in self.mtxRows if len(x) == len(self.mtxRows)]
                        index += 1
                for k in self.numElemRows:
                        # will continue...
                           
                           
        def _rangeMatrix(self):
                
        
        def _printMatrix(self):
                
                        
        def _determinantMatrix(self):
                
                for i in self.mtxRows:
                        # traverse the first array layer
